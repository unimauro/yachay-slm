.venv/
__pycache__/
*.pyc
data/distill/*.jsonl
checkpoints/
# tokenizer.json entrenado por el usuario (el del demo va en models/demo/)
/tokenizer.json
.env
.DS_Store

# Rust
rust/target/

# Fine-tune: datos regenerables y checkpoints intermedios (el adapter final SÍ se versiona)
finetune/data/
finetune/adapters/*_adapters.safetensors
finetune/yachay-general/

# El modelo demo SÍ se versiona (para "clonar y correr"):
!models/demo/
